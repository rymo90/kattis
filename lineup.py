x = int(input())
status= ""
prev = ""
for _ in range(x):
    nm= input()
    if prev == "":
        prev = nm
    else:
        if prev[0] > nm[0]:
            prev = nm
            status = "DECREASING"
        elif prev[0] == nm[0]:
            j = 1
            while len(nm) > 0:
                if nm[]
            for j in range(len(nm)):
                if nm[j+1]
